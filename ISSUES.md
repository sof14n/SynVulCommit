# Planned Issues

These are the first GitHub issues to create after publishing the repository.

1. Strip thinking-model reasoning before JSON parsing
2. Add production mode that refuses mock provider
3. Persist provider/model/prompt metadata in VUDENC exports
4. Add JSON schema validation for model outputs
5. Add run summary metrics and dataset verifier command
6. Strengthen diversity checks for scale generation
7. Add resumable target-count generation per CWE
8. Add CI for strict mock validation with Bandit and Semgrep
